import 'package:flutter_test/flutter_test.dart';

// Smoke tests for companion_app.
// Full integration tests require a running backend.
void main() {
  test('placeholder — integration tests require a live server', () {
    expect(true, isTrue);
  });
}
